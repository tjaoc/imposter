---
description: Al pedir commit/push, actualizar versión en todo el proyecto (auto-bump si no indica versión) y luego commit y push
alwaysApply: true
---

# Commit y push con versión

Cuando el usuario pida hacer **commit** y **push**:

1. **Si indica una versión** (ej. "commit push v1.1.7", "haz commit v1.2.0 y push"): usar esa versión (X.Y.Z) en todos los archivos listados abajo.
2. **Si NO indica versión** (ej. "commit y push", "haz commit y push"): leer la versión actual de `frontend/package.json` (ej. 1.1.6) y **subir el patch en 1** (1.1.6 → 1.1.7). Usar esa nueva versión en todos los archivos.

Después de actualizar la versión, hacer `git add` de los archivos de versión y de los demás cambios, `git commit -m "vX.Y.Z - ..."` (incluir resumen de cambios si hay otros), `git push`.

## Archivos a actualizar con la versión (X.Y.Z)

1. **VERSION.md**
   - Título: `# 🎮 Imposter - Version X.Y.Z`
   - En "Resumen de la Versión": primera línea `**vX.Y.Z** –` (descripción breve o genérica).
   - Pie: `**¡Gracias por probar Imposter vX.Y.Z!**`

2. **frontend/package.json** → `"version": "X.Y.Z"`

3. **backend/package.json** → `"version": "X.Y.Z"`

4. **CHANGELOG.md**
   - Añadir al inicio (después de `# 📝 Changelog - Imposter`) una sección:
     - `## [X.Y.Z] - AAAA-MM-DD` (fecha de hoy).
     - Una o dos líneas de cambios (si el usuario no da detalle, poner algo genérico como "Mejoras y correcciones" o resumir los archivos modificados).

No modificar `package-lock.json` ni versiones de dependencias; solo la versión del **proyecto** en los archivos listados.
