# Desarrollo: proxy de Vite hace /api y socket a localhost:4000 (no hace falta definir nada).
# Producción (Vercel): definir la URL del backend (tu subdominio API).

# URL del backend (API + Socket). Ejemplo con subdominio:
# VITE_API_URL=https://api.tudominio.com
# VITE_SOCKET_URL=https://api.tudominio.com
